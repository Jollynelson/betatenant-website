name = "betatenant-website"
compatibility_date = "2025-12-01"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = ".open-next"

# Cloudflare Pages needs _routes.json to know which paths hit the Worker vs static assets
# Without it, all routes hit the Worker including static files, causing potential issues
